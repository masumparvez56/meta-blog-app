import React from 'react';
import Banner from '../components/Banner';

const Home = () => {
    return (
        <section className='section-container'>
           <Banner/>
        </section>
    );
};

export default Home;